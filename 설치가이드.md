# TRPG 플랫폼 설치 가이드

**Supabase**(Postgres 기반 백엔드, 무료 티어)를 캐릭터/세션 DB이자 API 서버로 쓰고, 실제 화면(주사위 굴리기, 캐릭터 시트, 세션 로그, GM 도구)은 **GitHub Pages에 올린 정적 웹페이지**가 담당하는 구조입니다. 변경 사항은 Supabase Realtime을 통해 수십~수백ms 안에 모든 접속자 화면에 즉시 반영됩니다(폴링 대기 없음).

> 예전에는 구글 스프레드시트+Apps Script를 백엔드로 썼습니다. Apps Script는 호출 하나하나에 구조적인 고정 지연이 있어서 아무리 프론트를 최적화해도 체감 렉이 남아있었고, 그래서 Supabase로 옮겼습니다. 예전 Apps Script 배포 절차가 필요하면 이 문서 맨 아래 "레거시" 섹션을 참고하세요.

배포는 두 파트로 나뉩니다 — **① 백엔드(Supabase)** 를 먼저 만들어서 URL/키를 받고, **② 프론트엔드(GitHub Pages)** 에서 그 값을 바라보도록 설정합니다. 순서가 중요합니다.

## 1부. 백엔드(Supabase) 만들기

### 1. Supabase 프로젝트 생성

1. https://supabase.com 에서 계정을 만들고 로그인합니다(본인이 직접 해야 하는 단계입니다).
2. **New project**로 새 프로젝트를 만듭니다. 이름/비밀번호/리전은 자유롭게 정하면 됩니다(리전은 한국과 가까운 곳을 고르면 응답이 더 빠릅니다).

### 2. 테이블/보안 설정 적용

1. 왼쪽 메뉴에서 **SQL Editor**를 엽니다.
2. 전달드린 [supabase/schema.sql](supabase/schema.sql) 파일 내용을 전체 복사해서 붙여넣고 **Run**을 누릅니다.
3. 이 스크립트가 캐릭터/NPC_몬스터/솔로_캐릭터/세션로그/주사위기록/게임상태 6개 테이블을 만들고, 로그인 없이 링크만 있으면 읽고 쓸 수 있도록 접근 권한을 설정하고(예전 Apps Script "Anyone" 배포와 동일한 신뢰 모델입니다), Realtime(실시간 반영)까지 켜줍니다. 샘플 캐릭터 1개도 같이 들어갑니다.

### 3. URL과 anon key 확인

1. 왼쪽 메뉴 **Project Settings > API**로 이동합니다.
2. **Project URL**과 **anon public** 키, 이 두 값을 복사해두세요 — 2부에서 프론트 설정에 씁니다. (anon key는 "공개해도 되는" 키입니다 — RLS 정책이 실제 접근 범위를 제한합니다. 그래도 이 URL/키는 아는 사람만 접근 가능하니 플레이어에게는 2부의 GitHub Pages 주소만 공유하세요.)

### 4. 백엔드를 나중에 수정했을 때

테이블 구조를 바꾸고 싶으면 SQL Editor에서 필요한 `alter table` 문을 실행하면 됩니다. URL/anon key는 프로젝트를 지우지 않는 한 바뀌지 않습니다.

## 2부. 프론트엔드(GitHub Pages) 배포

### 1. Supabase 설정 채우기

`docs/index.html`을 열어 상단 근처의 아래 두 줄을 찾아서, 1부 3번에서 받은 값으로 바꿉니다.

```js
const SUPABASE_URL = 'https://REPLACE_ME.supabase.co';
const SUPABASE_ANON_KEY = 'REPLACE_ME';
```

### 2. GitHub 저장소에 올리기

이 폴더(`docs/`, `supabase/` 포함)를 GitHub 저장소로 push합니다. (저장소가 아직 없다면 새로 만들어서 push — anon key는 공개돼도 되는 키라 공개(Public) 저장소로 만들어도 안전합니다.)

### 3. GitHub Pages 켜기

1. 저장소 **Settings > Pages**로 이동.
2. **Source**를 **Deploy from a branch**로, **Branch**를 `main` / 폴더는 **`/docs`** 로 지정하고 저장.
3. 잠시 후 `https://<계정명>.github.io/<저장소명>/` 형태의 주소가 활성화됩니다. **이 주소가 이제 플레이어들에게 공유할 TRPG 플랫폼 URL입니다.**

### 4. 프론트엔드 코드를 나중에 수정했을 때

`docs/index.html`을 수정한 뒤 GitHub에 다시 push하면, GitHub Pages가 자동으로 최신 버전을 반영합니다. 별도 재배포 절차가 없습니다.

## 사용 방법 요약 (Roll20 / Foundry VTT / D&D Beyond / Owlbear Rodeo의 UX를 참고해 재구성)

화면 구조는 캐릭터 시트 / 솔로 플레이 / 활동 / GM 도구, 총 4개 탭 + 어디서나 뜨는 주사위 버튼으로 되어 있습니다. 앞의 세 항목(캐릭터 시트·활동·GM 도구)은 파티 플레이용이고, "솔로 플레이" 탭은 혼자 진행하는 D&D 기반 원샷 모험 전용입니다 (자세한 내용은 아래 "솔로 플레이 모드" 항목 참고).

- **캐릭터 시트 탭**: 목록에서 캐릭터를 고르면 오른쪽에 이름·직업·레벨·HP가 항상 보이는 요약 바가 뜨고, 그 아래 "개요"(스탯 등 핵심 정보)와 "상세 정보"(스킬·소지품·배경) 두 개의 서브탭으로 나뉩니다 — D&D Beyond 캐릭터 시트처럼 자주 보는 정보는 스크롤 없이, 세부 정보는 탭 뒤에 숨겨서 필요할 때만 열어보는 구조입니다. 스탯은 이름-값 쌍을 자유롭게 추가/삭제할 수 있어 어떤 시스템이든 쓸 수 있고, HP는 +1/+5/-1/-5 버튼으로 빠르게 조정됩니다.
- **주사위 굴리기 (플로팅 버튼)**: 화면 우측 하단의 동그란 주사위 버튼은 어느 탭에 있든 항상 떠 있습니다. 눌러서 위로 펼쳐지는 패널에서 굴리고, 굴린 뒤 그대로 캐릭터 시트든 GM 도구든 하던 걸 계속 볼 수 있어요 (Roll20·Foundry의 "채팅/주사위 패널이 항상 켜져 있는" 방식과 Owlbear Rodeo의 "필요할 때만 여는 오버레이" 방식을 섞은 형태입니다). `1d20`, `2d6+3`, `4d6kh3`(가장 높은 3개 유지), `2d20kh1`(어드밴티지), `2d20kl1`(디스어드밴티지) 등을 지원하고, natural 20/1은 자동으로 크리티컬 배지가 뜹니다.
- **활동 탭**: 세션 로그(내레이션/대사/판정/기타)와 방금 굴린 주사위 결과가 한 개의 시간순 피드로 합쳐져서 보입니다 (Foundry·Roll20의 "채팅 하나에 모든 게 쌓이는" 방식). 위쪽 필터 칩으로 "판정만" "주사위만" 처럼 즉시 걸러볼 수 있고, 다른 탭에 있을 때 새 활동이 생기면 탭 옆에 작은 점으로 알려줍니다.
- **GM 도구 탭**: "GM 모드 켜기"를 누르면 현재씬/라운드/공지 편집, **전투 순서(이니셔티브) 트래커**, NPC·몬스터 관리(HP 트래커 포함)가 나타납니다. 전투 순서는 이름과 선제값을 입력해 추가하면 자동으로 높은 순서로 정렬되고, 현재 턴인 참가자는 파란 테두리로 강조됩니다. "다음 턴" 버튼을 누르면 순서대로 넘어가고, 마지막 사람 다음에 다시 처음으로 돌아갈 때 라운드 숫자가 자동으로 하나 올라갑니다 (Foundry VTT의 전투 트래커와 같은 방식). 이 탭 전체는 화면을 가리는 용도일 뿐 진짜 보안 장치는 아니니, 정말 플레이어에게 숨기고 싶은 정보는 GM 본인만 따로 웹앱을 열어서 보는 걸 권장합니다.

### 휴대폰/좁은 화면에서 쓸 때

가로 폭이 좁아지면(휴대폰 등) 자동으로 레이아웃이 바뀝니다. 상단 탭 대신 화면 하단에 캐릭터/활동/GM 3개 아이콘이 있는 네비게이션 바가 뜨고, 캐릭터 탭은 "목록"과 "상세/편집" 두 화면을 위쪽 토글 버튼으로 오가는 방식으로 바뀌어 좁은 화면에서도 스크롤이 길어지지 않습니다. 주사위 버튼은 하단 네비게이션 위쪽에 겹치지 않게 자리를 잡습니다.

## 솔로 플레이 모드 (1인 진행)

파티 없이 혼자서도 D&D 기반의 짧은 모험을 진행할 수 있는 전용 탭이 추가되었습니다.

- **캐릭터 생성 위저드**: "솔로 플레이" 탭의 "+ 새 모험가 만들기"를 누르면 발더스 게이트 3 스타일의 단계별 위저드가 열립니다. 종족 → 클래스 → 배경 → 능력치(포인트 바이, 총 27점) → 이름·외형 → 최종 확인 순서로 진행하며, 각 단계는 카드를 클릭해 선택하는 방식입니다. 완료하면 자동으로 HP·AC가 계산되어 모험가가 만들어집니다.
- **시나리오 선택**: 새로 만든 모험가로 "모험 시작"을 누르면(진행 중인 이야기가 없을 때) 어떤 원샷 시나리오로 시작할지 고르는 화면이 뜹니다. 이미 진행 중인 이야기가 있으면 고른 시나리오 그대로 이어서 진행됩니다.
  - **"안개 낀 숲"**: 실종된 상단을 좇아 안개 낀 숲으로 들어가는 분기형 이야기입니다. 야영지 조사, 늑대 조우 같은 곁가지 장면이 있고, 광신도를 물리친 뒤 남긴 일지를 해독하는지에 따라 "숲의 진실을 밝히다"보다 더 높은 결말인 "전설이 된 자"까지 갈릴 수 있습니다. 그 외에도 "무사 귀환", "안개 속으로"(여운이 남는 결말), "쓰러지다"(패배) 등 다양한 결말로 이어집니다.
  - **"안개 항구의 실종자들"**: 항구 마을에서 사라지는 사람들의 배후를 쫓는 새 원샷 시나리오입니다. 선술집 정보 수집, 잠입/정면돌파 선택, 지하 제단의 위험한 유물 처리 방식에 따라 결말이 4가지로 갈립니다.
  - 결말 화면에서 "다시 도전하기"(같은 시나리오 재도전)와 "다른 이야기 선택하기"(시나리오 선택 화면으로 돌아가기) 중 고를 수 있습니다.
  - 장면마다 선택지를 고르거나, 능력치 판정(주사위)으로 성공/실패가 갈리거나, 간이 전투(공격 굴림 → 명중 시 피해 굴림 → 상대 반격)를 치르게 되는 구조는 두 시나리오가 동일합니다.
- **진행 상황 자동 저장**: 장면을 이동하거나 전투 한 라운드가 끝날 때마다 진행 상황이 자동 저장됩니다. 중간에 다른 탭으로 이동했다가 돌아와도, 목록에서 같은 캐릭터의 "모험 시작"을 다시 누르면 이어했던 장면부터 계속됩니다. "다시 도전하기"를 누르면 HP를 모두 회복하고 처음부터 새로 시작합니다.
- **시트 자동 생성**: 솔로 캐릭터는 `솔로_캐릭터`라는 별도 탭에 저장되는데, 스프레드시트에 이 탭이 아직 없어도 처음 캐릭터를 만드는 순간 자동으로 만들어집니다. 직접 탭을 추가할 필요가 없습니다. 진행 상황(현재 장면, 선택 기록 등)은 기존 `게임상태` 탭에 `솔로진행_<캐릭터ID>` 키로 저장되어 별도 시트가 필요 없습니다.
- **GM 진행 방식에 대한 참고**: 이 화면(파티 공유 배포)의 "GM"은 실시간으로 응답을 만들어내는 AI가 아니라, 미리 작성해 둔 분기형 시나리오입니다. 매 턴 AI를 호출하는 방식이 아니기 때문에 별도의 API 비용 없이 지금 배포한 그대로 바로 쓸 수 있습니다. 시나리오는 현재 "안개 낀 숲"과 "안개 항구의 실종자들" 두 편이 들어 있으며, 이후 새로운 원샷 시나리오를 요청하시면 같은 방식으로 추가해 드릴 수 있습니다. 매 턴 AI가 즉석에서 이야기를 지어내는 방식을 원하시면 아래 "개인용 생성형 AI 모드"를 참고하세요.

## 개인용 생성형 AI 모드 (선택, 파티와 비공유)

위 솔로 플레이는 미리 써둔 시나리오를 따라가는 방식이지만, 매번 다른 이야기를 즉석에서 만들어주는 방식을 원하시면 별도로 준비된 개인용 모드를 쓸 수 있습니다. **토큰 비용이 실제로 발생하고, 지금 파티원들과 공유 중인 GitHub Pages 배포와는 완전히 분리된 개인 전용 도구**입니다 — 공유 배포에는 이 기능이 전혀 포함되어 있지 않습니다.

1. **개인용 Apps Script 배포**: `personal/GeminiProxy.gs` 파일 상단 주석을 따라, 스프레드시트에 바인딩하지 않는 **새 독립 Apps Script 프로젝트**를 만들고 내용을 붙여넣습니다. 프로젝트 설정 > 스크립트 속성에서 `GEMINI_API_KEY`에 본인의 Gemini API 키(https://aistudio.google.com 에서 발급)를 등록한 뒤, 웹앱으로 배포해 나온 `/exec` URL을 본인만 보관합니다. (이 URL을 다른 사람에게 알려주면 그 사람도 이 키로 토큰을 쓸 수 있게 됩니다.)
2. **개인용 페이지 실행**: `personal/ai-solo.html`을 다운로드해 브라우저로 직접 엽니다(더블클릭). 별도 호스팅이 필요 없습니다. 처음 열면 위에서 받은 프록시 URL을 입력하라는 화면이 뜨는데, 한 번 입력해두면 그 브라우저에 저장되어 다음부터는 바로 이야기를 시작할 수 있습니다.
3. 배경/캐릭터/톤을 자유롭게 적어 이야기를 시작하면, 그때그때 Gemini가 새로운 서술을 만들어줍니다. 주사위나 HP 같은 규칙은 이번 버전에는 없는 순수 서술형이며, 대화가 길어지면 오래된 내용은 자동으로 잘라내 토큰 사용량을 억제합니다.

## 최근 수정 사항

- **[구조 변경] 백엔드를 Google Sheets/Apps Script → Supabase로 전환** — 폴링 배치, 배경 애니메이션 정지 등으로도 해소되지 않던 체감 렉의 근본 원인이 Apps Script 호출당 고정 지연이었습니다. Supabase(Postgres)로 옮기면서 폴링 자체를 없애고 Realtime 구독으로 바꿔, 다른 사람의 변경이 수십~수백ms 안에 반영됩니다. 화면/기능은 전혀 바뀌지 않았습니다 — 데이터 연결부만 교체됐습니다. 이 문서의 "1부/2부" 안내대로 **다시 배포**해야 하고, 플레이어에게 공유하는 GitHub Pages 링크 자체는 바뀌지 않습니다.
- **[성능] 폴링을 배치 요청으로 통합** — (Apps Script 시절 개선사항, 지금은 Realtime으로 대체됨) — 예전에는 캐릭터/주사위기록/세션로그/[GM모드면]NPC+게임상태를 6초·8초·12초·12초 간격의 서로 다른 4개 타이머로 각각 따로 불러왔습니다. Apps Script 호출 하나하나에 고정 지연이 있다 보니 이게 겹쳐 돌 때마다 체감 렉이 쌓였는데, 이제 `getFeedSnapshot` 하나로 묶어 10초 간격 요청 1번으로 줄였습니다. 데이터가 실제로 안 바뀌었으면 화면도 다시 그리지 않아 불필요한 깜빡임도 줄었습니다. (백엔드 재배포 필요)
- **[콘텐츠] 솔로 시나리오 확장 + 시나리오 선택 화면 추가** — "안개 낀 숲"에 야영지 조사, 늑대 조우, 광신도의 일지 해독 같은 곁가지와 새 결말("전설이 된 자")을 추가했고, 완전히 새로운 원샷 시나리오 "안개 항구의 실종자들"이 생겼습니다. 이제 모험을 시작할 때 어떤 이야기로 갈지 고르는 화면이 먼저 뜹니다. 기존에 진행 중이던 캐릭터의 저장 데이터는 그대로 "안개 낀 숲" 이어하기로 인식되어 하위 호환됩니다.
- **[신규] 개인용 생성형 AI 모드** — 위 "개인용 생성형 AI 모드" 섹션 참고. 파티 공유 배포와는 완전히 분리된 별도 도구입니다.
- **[구조 변경] 백엔드/프론트엔드 분리** — "사이트가 무겁다"는 피드백에 대한 3안(A/B/C) 중 하이브리드(B안)를 적용했습니다. Apps Script는 더 이상 화면을 서빙하지 않고 JSON API 역할만 하며, 화면(`index.html`)은 GitHub Pages 같은 정적 호스팅에서 별도로 서빙됩니다. 기존에 Apps Script 프로젝트 안에 `index.html`을 추가하는 방식으로 배포하셨다면, 이 문서의 "1부/2부" 안내대로 **다시 배포**해야 합니다 — 특히 배포 방식(웹앱 URL을 그대로 여는 방식 → GitHub Pages 주소를 여는 방식)이 바뀌었으니 플레이어에게 공유하는 링크도 바뀝니다.
- **주사위 굴리기가 활동 피드에 바로 안 뜨던 문제**를 고쳤습니다. 솔로 플레이 중 판정/전투에서 굴린 주사위도 이제 파티 모드처럼 굴리는 즉시 활동 피드용 데이터를 다시 불러오고, 활동 탭이 아니면 안 읽음 표시(점)가 바로 뜹니다. (예전에는 최대 6초 뒤 자동 새로고침 때까지 기다려야 보였습니다.)
- **스프레드시트를 직접 수정했을 때 캐릭터 목록이 통째로 안 보이던 문제**를 예방하는 방어 코드를 추가했습니다. 특정 한 행의 값이 이상해도(예: 이름 칸에 숫자만 입력됨) 그 행 때문에 목록 전체가 사라지지 않고, 나머지 캐릭터는 정상적으로 보입니다. 문제가 있는 행은 브라우저 콘솔(F12)에 어떤 캐릭터인지 로그로 남습니다. 이 문제가 다시 발생하면 F12 콘솔의 오류 메시지와 `솔로_캐릭터` 시트 1행(헤더)이 `ID, 캐릭터명, 종족, 클래스, 배경, 능력치(JSON), 현재HP, 최대HP, 스킬/특기, 소지품, 외형, 골드, 최종수정`과 정확히 일치하는지 확인해주시면 원인을 더 좁힐 수 있습니다.
- **체감 무게 개선(1차)**: 브라우저 탭이 백그라운드에 있을 때(다른 탭/창을 보고 있을 때)는 자동 새로고침을 쉬도록 해서 불필요한 서버 호출을 줄였습니다. 탭으로 돌아오면 즉시 한 번 새로고침해 최신 상태를 바로 보여줍니다.

## 알아두면 좋은 점

- 여러 명이 같은 URL을 동시에 열어도 됩니다. 각자의 조작은 Supabase Realtime을 통해 수십~수백ms 안에 다른 사람 화면에도 반영됩니다(예전 시트 시절의 "몇 초 기다려야 보이던" 지연이 없습니다). 아주 미세한 타이밍 충돌(같은 순간 같은 값 동시 수정)은 이론상 가능합니다 — 소규모 고정 파티 용도로는 문제 없는 수준입니다.
- 데이터를 표 형태로 직접 보거나 고치고 싶으면 Supabase 대시보드의 **Table Editor**에서 할 수 있습니다(구글 스프레드시트를 직접 열어 고치던 것과 비슷한 감각입니다). 전투 순서는 `게임상태` 테이블에 `전투순서`/`현재턴` 키로 JSON 형태로 저장됩니다.
- `스탯(JSON)` 칸은 형식이 깨지면 빈 값으로 처리되니, 웹앱의 "스탯 추가" 버튼으로 편집하는 걸 권장합니다.
- 백업이 필요하면 Supabase 대시보드의 **Database > Backups**(무료 티어는 제한적) 또는 SQL Editor에서 `select * from "캐릭터";` 같은 쿼리로 직접 내보낼 수 있습니다.

## 레거시: Apps Script 배포 (더 이상 사용 안 함)

지금은 위 "Supabase" 절차를 따르면 됩니다. 아래는 참고/롤백용으로만 남겨둔 예전 절차입니다.

1. 구글 스프레드시트에서 **확장 프로그램 > Apps Script**를 열고, [backend/Code.gs](backend/Code.gs) 내용을 붙여넣습니다(이 파일은 참고용으로만 저장소에 남아있고 더 이상 관리되지 않습니다).
2. **배포 > 새 배포 > 웹앱**, 실행 계정: 나, 액세스: Anyone으로 배포해 `/exec` URL을 받습니다.
3. `docs/index.html`에서 `SUPABASE_URL`/`SUPABASE_ANON_KEY` 대신 예전 `API_URL` 상수와 fetch 기반 `callApi_` 구현을 되살려야 합니다(현재 코드는 Supabase 전제로 되어 있어 그대로는 안 붙습니다) — 롤백이 필요하면 git 커밋 이력에서 Supabase 전환 이전 버전을 참고하세요.
